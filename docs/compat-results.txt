# Verified compatibility results.
#
# One line per environment where scripts/compat-probe.sh has actually loaded the
# sensors, pasted verbatim from its output. This file is evidence, not
# documentation: docs/COMPATIBILITY.md may only mark a row "verified" if a
# matching line exists here, which tests/docs_consistency.rs enforces.
#
# Thirteen of that table's rows are inferred from shipped kernel versions and
# default configs. This is how they get retired -- one real run at a time,
# including runs that show the tool working less well than the table guessed.
#
# To add one:
#   sudo ./scripts/compat-probe.sh
# and paste the ks-compat line below.

# Superseded: measured against an 11-sensor build, before lsm/path_mknod was
# added. Kept because a compatibility claim should show its history rather than
# be quietly replaced -- but not read as current, and not counted by the tests.
# Re-run scripts/compat-probe.sh on each to restore them.
#
# A result line names a distribution and a kernel, which is the point of the
# table -- but it is also a fact about somebody's machine. Send one only if you
# are content for it to be public, and note the probe reports nothing else: no
# hostname, no username, no addresses, no file contents.
# ks-compat: distro=kali-2026.2 kernel=7.0.12+kali-amd64 arch=x86_64 bpflsm=active sensors=11/11 missing=none live=exec,path_chmod,file_open
# ks-compat: distro=ubuntu-24.04 kernel=6.17.0-1022-azure arch=x86_64 bpflsm=inactive sensors=5/11 missing=file_open,path_chmod,inode_setxattr,ptrace,bprm_check,socket_connect live=exec
